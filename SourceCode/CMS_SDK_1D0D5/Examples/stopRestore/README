这个例程用于测试STOP复归时间，即从深度睡眠到唤醒CPU的时间。
改变TRMW的值，可以改变复归时间。

各个管脚的用途如下：
  P50/INTP1下降沿中断功能，用于唤醒CPU
  P72/PCLBUZ0输出系统时钟的8分频时钟，用于观测系统时钟起振时间
  P15/PCLBUZ1输出副系统时钟的分频时钟，连接到P50/INTP1用于唤醒CPU
  P52作为观测唤醒的标志，在每次唤醒CPU后翻转一次。

注意：请把P15/PCLBUZ1连到P50/INTP1用于唤醒CPU系统。